<?php
/*$id = filter_input(INPUT_GET, 'id', FILTER_VALIDATE_INT);
 if ($id===null || $id === false || !file_exists("models/game_$id.php")) {
   exit();
 }
 $title=$team_names[$id];
include "models/team_names.php";
include "models/game_$id.php";

function goal_counts($position) {
global $game;
$count = 0;
foreach($game['goals'] as $goal)
if ($goal['team']==$position)
$count++;
return $count;
}

$goal_counts = [goal_counts(0), goal_counts(1)];
 
$view="game" ; 
include 'views/page.php';


include 'controllers/Soccer.php';
 $controller = new Soccer();*/


?>
